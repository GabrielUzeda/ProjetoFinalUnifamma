<div class="features-grid">
    {{#dados}}
    <div class="feature-card">
        <div class="feature-icon">{{icone}}</div>
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
    </div>
    {{/dados}}
</div> 