<div class="pricing-grid">
    {{#dados}}
    <div class="pricing-card {{#destaque}}featured{{/destaque}}">
        <h3>{{nome}}</h3>
        <div class="price">R$ {{preco}}<span>/total</span></div>
        <p>{{descricao}}</p>
        <ul class="plan-features">
            {{#caracteristicas}}
            <li>{{.}}</li>
            {{/caracteristicas}}
        </ul>
        <a href="#contact" class="btn">Solicitar Orçamento</a>
    </div>
    {{/dados}}
</div> 